# glEYEco Galectin-3 Computational Validation (Progress)

Currently, the folder holds the first step of the glEYEco project. The goal was to use real glycan binding databases to answer two questions before doing any wetlab work:

1. Are the right glycans actually present at the eye surface? (Ex: does the cornea have enough Core 1 O-glycans for Gal-3 to grab onto)
2. Will Gal-3 bind the right ones and not the wrong ones? (Ex: will it stick to corneal mucins but leave ECM proteins and ubiquitous N-glycans alone)

Everything is in: galectin3_analysis.ipynb
The other files are outputs from that notebook or archived older work.

# What's in this folder

| File / Folder | What it is |
|---------------|------------|
| `galectin3_analysis.ipynb` | The main analysis notebook — run this |
| `docking_targets_smiles.csv` | SMILES chemical strings for the on/off-target glycans, generated by Section 5 |
| `glycan_pdbs/` | 3D `.pdb` structure files for docking simulations, generated by Section 5 |
| `fig_*.png` | Plots saved by the notebook when you run it |
| `gal8_archive/` | Old version of the analysis using Galectin-8 instead of Gal-3, kept for reference and comparison, we will be using this in the future |
| `README.md` | The file that contains all of the information needed to recreate everything. |

# The two databases the notebook uses

- `df_glycan` (~50,500 glycan sequences) — a catalogue of known glycan structures with tissue and disease associations. Used to answer: *which glycans exist at the eye?*
- `glycan_binding` (~2,100 proteins × 2,900 glycans) — experimental binding scores from glycan microarray data. Used to answer: *what does Galectin-3 actually bind?*

One thing to know: these two databases don't always use the same naming conventions for glycans, so direct matches between them often come up empty. When that happens the notebook falls back to motif-level searching (e.g. "count how many ocular glycans contain the pattern `Gal(b1-3)GalNAc`") which is still meaningful.

# On-target vs off-targets (Results)

| Class | Glycan | Why it matters |
|-------|--------|----------------|
| **On-target** | Core 1 O-Glycan `Gal(b1-3)GalNAc` | The mucin O-glycan Gal-3 should anchor to on the corneal surface |
| Off-target | LacNAc `Gal(b1-4)GlcNAc` | Extremely common — shows up in almost every N- and O-glycan in the body |
| Off-target | Fibronectin / Laminin N-glycans | ECM proteins in the stroma — non-specific binding here = unintended retention |
| Off-target | Galactosylated bi-antennary N-glycan | The most common complex N-glycan structure; carries LacNAc arms |
| Neg ctrl | Tn Antigen `GalNAc` | The biosynthetic step *before* Core 1 — no Gal attached, Gal-3 shouldn't bind this |

# How to run it

Install dependencies (one-time):

```bash
pip install glycowork
pip install "glycowork[ml]"     # only needed for Section 4 (LectinOracle predictions)
pip install "glycowork[chem]" rdkit   # only needed for Section 5 (SMILES + 3D structures)
```

Open the notebook:

```bash
jupyter notebook galectin3_analysis.ipynb
```

Run all cells top to bottom. Sections 4 and 5 have optional dependencies — the notebook checks for them and skips gracefully if they're not installed.

# What each section of the notebook does

Section 1  Setup: installs packages, imports everything.

Section 2  Validate target tissue:** searches `df_glycan` for glycans tagged to eye/cornea/lacrimal tissue. Finds ~134. Breaks them down by type and checks which ones carry β-galactoside linkages (the motif Gal-3 recognises).

Section 3  Gal-3 specificity: pulls Galectin-3 binding data from `glycan_binding`, cross-references with the ocular glycan set, and checks how many ocular glycans carry the Core 1 and LacNAc motifs. Section 3.8 is the on-target vs off-target comparison added to quantify selectivity risk.

Section 4  LectinOracle (optional): an ML model that predicts binding scores for a custom protein sequence. Useful once you have an engineered fusion protein sequence to test.

Section 5  Structures for docking: converts glycan names (IUPAC strings) → SMILES → 3D `.pdb` files. The four simple glycans generate fine; the large biantennary N-glycan needs to be done manually through [GLYCAM-Web](https://glycam.org/cb).

# References

- glycowork paper: [Thomes et al., 2021](https://academic.oup.com/glycob/advance-article/doi/10.1093/glycob/cwab067/6311240) | [GitHub](https://github.com/BojarLab/glycowork)
- LectinOracle: [Lundstrom et al., 2021](https://onlinelibrary.wiley.com/doi/10.1002/advs.202103807)
- 3D structure builder: https://glycam.org/cb
- Glycan registry: https://glytoucan.org

Notes on what to do until May 11 th.
- Step 1 & 2: understand the storyline:
We chose Gal 3 first because its the most studied,
it's also the simplest structure that works. 
(Find 1-2 literature that is cited by many for gal 3 that supports this idea and cite it in # Reference)

So the storyline for 2.1-2.4 goes like this:
Where? (corneal epithelium) 2.2
    ↓
What glycan type? (O-glycans) 2.4
    ↓
Which specific glycan? (Core 1 O Glycan on MUC 1 at Thr86 ) 2.3
    ↓
Is it disease-specific? (filtering — still TBD) 2.1 (wet lab and dry lab has not decided if this is possible or if it matters so for future directions before we move on we need to make a program that figures this out (if it matters))

Priority: Fix the ordering of 2.1-2.4,
make it so that it makes sense for the searches
under each map explain why we made that decision 3-5 sentences.

- Step 3:
Currently the motifs are useless 2.5 - 2.6
Keep them but don't use them, they might be used for docking later on but the specific reasoning from what i see is they have functions and we're trying to list them based on functions. This might be bad for us in the long run because we did not take this into consideration when we chose gal 3. So let's just leave it here for now, when we move it to git lab (igem web), we're going to probably pretend this never existed lol.
Possibly could do some research to find out if this has any other purposes other than for docking...

- Step 4:
Currently, Section 3 is trying to compare it to other gal families proving it's the best, perhaps because it used to be gal 8, this whole data set is incorrect. Instead it uses a different IUPAC annotation between the databases, meaning the gal 3 we're using is different from the gal 3 that's comparing. Hence the reason why we don't get a result, instead it just states what our off targets are and how gal 3 is present in 78 and 8 ocular glycans, (just facts, no results) We need to redo this whole section, keeping only 3.7 (write a writeup on why this is important under it), 3.8. When your redoing this whole thing, do not compare it with our off targets for 3.8, instead the storyline should be 2 parts. part 1: Find off targets, check binding affinity, check structural affinity, results being 3.8 in a list, then we'll add more. part 2: ensures gal 3 binds optimally with our fusion protein, which is yet to be decided so leave this after section 1. (Throughout this whole process do not delete anything, I would just make a copy of gal 3 analysis.ipynb, because there's a chance we're still going to use it.) 